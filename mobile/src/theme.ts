/**
 * @deprecated Import from `@/src/theme/index` instead for full theme support.
 * This file re-exports the static light tokens for backward compatibility.
 */
export { lightColors as colors, radius, spacing, cardShadowLight as cardShadow, healthScoreColor, scoreLabel } from "./theme/index";
